import { Currency2wordsPipe } from './currency2words.pipe';

describe('Currency2wordsPipe', () => {
  it('create an instance', () => {
    const pipe = new Currency2wordsPipe();
    expect(pipe).toBeTruthy();
  });
});
